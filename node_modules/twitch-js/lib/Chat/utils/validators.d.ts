import * as types from '../types';
export declare const chatOptions: (maybeOptions: types.ChatOptions) => types.ChatOptions;
export declare const clientOptions: (maybeOptions: types.ClientOptions) => types.ClientOptions;
