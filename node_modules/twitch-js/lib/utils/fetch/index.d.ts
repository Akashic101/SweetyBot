import { RequestInit, RequestInfo } from 'node-fetch';
import { IStringifyOptions } from 'qs';
declare type SearchOptions = {
    /** Any query parameters you want to add to your request. */
    search?: {
        [key: string]: any;
    };
};
export declare type FetchOptions = RequestInit & SearchOptions;
/**
 * Fetches URL
 */
declare const fetchUtil: <T = any>(url: RequestInfo, options?: FetchOptions, qsOptions?: IStringifyOptions) => Promise<T>;
export default fetchUtil;
